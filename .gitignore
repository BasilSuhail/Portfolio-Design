node_modules
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.local
.env.production
dist

# Don't track gallery data (persisted via Docker volumes)
gallery.json
gallery_settings.json
gallery-data/*.json
!gallery-data/.gitkeep